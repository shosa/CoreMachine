{
  "name": "@coremachine/desktop",
  "version": "1.0.0",
  "private": true,
  "description": "CoreMachine Desktop App (Electron)",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "npm run build && electron .",
    "dev": "npm run watch"
  },
  "devDependencies": {
    "electron": "^29.1.5",
    "typescript": "^5.3.0"
  },
  "dependencies": {}
}
